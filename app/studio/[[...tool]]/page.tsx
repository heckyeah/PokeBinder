import { StudioClient } from "./StudioClient";

export const metadata = {
  title: "Pokemon Binder – Studio",
  robots: "noindex",
};

export default function StudioPage() {
  return <StudioClient />;
}
